spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    generate-ddl: false
  #    properties:
  #      hibernate:
  #        format_sql: true
  sql:
    init:
      schema-locations: classpath:/sql/init.sql
      mode: always
  mail:
    properties:
      mail:
        debug: true
        smtp:
          connectiontimeout: 5000
          starttls:
            enable: true
            required: true
          auth: false



#logging:
#  level:
#    org:
#      hibernate:
#        type:
#          descriptor:
#            sql: trace

async:
  threadNamePrefix: 'async-thread-'
  corePoolSize: 20
  maxPoolSize: 40
  queueCapacity: 400
  keepAliveSeconds: 30

