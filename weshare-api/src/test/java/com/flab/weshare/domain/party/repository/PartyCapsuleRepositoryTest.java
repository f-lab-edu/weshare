package com.flab.weshare.domain.party.repository;

import com.flab.weshare.domain.base.BaseRepositoryTest;

class PartyCapsuleRepositoryTest extends BaseRepositoryTest {
	// @Test
	// void cardTest() {
	// 	Page<PartyCapsule> fetchByStatus = partyCapsuleRepository.findFetchPartyCapsuleByStatus(
	// 		PartyCapsuleStatus.PRE_OCCUPIED, PageRequest.of(0, 2));
	// 	List<PartyCapsule> content = fetchByStatus.getContent();
	//
	// 	assertThat(fetchByStatus.getTotalElements()).isEqualTo(2);
	// 	//assertThat(content.get(0).getPartyMember().getCard().size()).isEqualTo(1);
	// 	//assertThat(content.get(0).getPartyMember().getCard().get(0).getCardStatus()).isEqualTo(CardStatus.AVAILABLE);
	// }
}
